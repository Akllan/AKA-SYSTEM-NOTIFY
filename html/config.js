const Config = {
    // Cambiar a true para ver los console.log en la consola del navegador NUI (F8 -> DevTools)
    Debug: false,
    // Color del HUD (purple, blue, yellow, green, red, cyan, pink, orange, white)
    HudColor: 'purple'
};
